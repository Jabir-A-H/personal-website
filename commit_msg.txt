refactor: implement group 1 refinement fixes

This commit addresses several layout, accessibility, and code quality issues identified in the website refinement plan.

UI/UX & Layout
- fix: make hero section naturally full-width, removing the `w-screen` hack. Updated `Shell.tsx` and all individual page wrappers to control their own width.
- fix: correct heading hierarchy (`<h2>` to `<h1>`) and add missing styles to `global-error.tsx`.
- feat: add "Private Repo" badge with a lock icon for non-public projects instead of rendering broken 404 source links. 
- fix: remove `md:hidden` from project descriptions in the "Other Projects" table so they are visible on desktop.
- fix: standardize education date separators in `data.json` to use em-dashes (`—`) to match the experience section format.
- fix: remove hardcoded footnotes from the education page.

Accessibility & SEO
- fix: remove misleading alt text from the decorative hero background image and add `aria-hidden="true"`.
- feat: add canonical URL to `app/layout.tsx` metadata to prevent duplicate-content indexing.
- feat: add `@id` to `PersonSchema` in `JsonLd.tsx` for better entity cross-referencing.

Code Quality
- refactor: standardize all relative imports (`../../`, `../`) to use the `@/` alias across all page components.
- refactor: make "Preferred" badges on the contact page data-driven via `data.json` instead of hardcoding endpoint names.
- fix: remove unnecessary `suppressHydrationWarning` from the `<body>` tag in layout.
